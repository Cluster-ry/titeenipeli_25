import * as ctf from "./ctf";
import * as login from "./login";
import * as map from "./map";
import * as users from "./users";

export const api = {
    ctf: ctf,
    login: login,
    map: map,
    users: users
}