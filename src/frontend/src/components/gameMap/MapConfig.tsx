/**
 * Client-side configuration that covers the following:
 * 1) Map width and height
 * 2) Size of an individual pixel
 */
export const mapConfig = {
    MapWidth: 256,
    MapHeight: 256,
    PixelSize: 32,
};
