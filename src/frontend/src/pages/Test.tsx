/**
 * A test page, currently used for testing the frontend API client.
 * @NOTE Should not be included in production 
 */

import { ApiClient } from "../api/api"

function Test() {

    return <div>

    </div>
}